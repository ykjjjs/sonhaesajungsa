# CLAUDE.md — 손해사정사 1차 학습 서비스

손해사정사 1차 5개년 기출 **600문항**과 3과목 **전자교재 55절**을 파는 서비스.
Cloudflare Workers + D1 + KV. 외부 라이브러리 0.

## 시작하기

```bash
npm i                # playwright (검증용). 파이썬은 표준 라이브러리만 씀
npm run build        # content/ → public/ + data/ + dist/preview/
npm run verify       # Playwright 4종 (약 2~3분)
npm run serve        # public/ 을 :8788 로
```

빌드는 항상 통과해야 하고, 화면을 건드렸으면 `npm run verify` 까지 통과시킨 뒤 커밋한다.

## 구조

```
app/           HTML 템플릿 — 여기를 고친다 (public/ 은 산출물)
content/       교재 본문·용어사전 파이썬 모듈
build/         빌드 스크립트 (paths.py 가 모든 경로의 기준)
test/          Playwright 검증
tools/         PDF 문항 추출 (이력용, 평소엔 안 씀)
data/          exam.json · sample_exam.json · book.json → KV 로 올림
public/        빌드 산출물 — 직접 고치지 말 것
worker/        worker.js · schema.sql
dist/          미리보기·아티팩트 (gitignore)
```

### 절대 잊지 말 것

- **`public/*.html` 을 직접 고치지 않는다.** `app/` 템플릿을 고치고 `npm run build`.
  직접 고치면 다음 빌드에 통째로 덮인다.
- **`wrangler.toml` 은 저장소 루트에 둔다.** Cloudflare 빌드가 루트에서 돈다.
- **템플릿을 인덱스로 잘라 붙이는 패치 금지.** 구분자 문자열이 CSS 에도 나타나
  빈 슬라이스가 만들어지면 `str.replace("", …)` 가 글자마다 삽입되어 파일이 폭발한다
  (실제로 48MB 사고가 났다). 패치는 반드시 이렇게 한다.
  ```python
  assert s.count(old) == 1      # 유일성 먼저 확인
  s = s.replace(old, new)
  ```
- **PBKDF2 반복은 100,000 을 넘기지 않는다.** Workers 실환경 상한이라 넘기면
  가입·로그인이 통째로 실패한다.
- **가격을 공인중개사(5,500원)와 같게 만들지 않는다.** 같은 안드로이드 공기계로
  두 서비스의 입금 알림을 받는데, **금액이 서비스 구분자**다. 지금은 9,900원.
- **한글은 UTF-8 그대로** 쓴다. `\uXXXX` 이스케이프 금지.
- **인라인 `onclick` 금지.** 이벤트 위임으로 처리한다.
- localStorage 에 민감 정보를 넣지 않는다. 토큰과 풀이 기록만.

## 콘텐츠가 새어 나가지 않게 하는 구조

이 서비스의 핵심 제약이다. 건드릴 때 반드시 이해하고 손댈 것.

```
              미결제                        이용권 ON
public/index.html   내장 120문항(2026)      /api/content → 600문항
public/book.html    본문 없음(BOOK=null)     /api/content → 55절
```

- `build/build_app.py` 가 배포본에는 **무료 회차만** 박고, 전체는 `dist/preview/` 에만 박는다
- `build/build_book.py` 가 배포본 `book.html` 은 `__BOOK__` 을 `null` 로 채운다
- `/api/content` 는 `activePaid(u)` 를 확인하고 미결제면 **402**
- 무료 회차와 가격은 `build/paths.py` 의 `FREE_YEAR` · `PRICE` 한 곳에서 바꾼다

## 결제 — 무통장입금

PG 없음. 수수료 0원.

```
가입 → /api/pay/request 가 4자리 코드 발급
   → 고객이 "이름+코드" 로 9,900원 입금
   → 공기계 토스 알림을 MacroDroid 가 /api/hook/deposit 으로 전송
   → 코드 + 금액이 모두 맞으면 자동 승인 (1년, 기존 기간에 연장)
   → 안 잡히면 /admin.html 에서 수동 승인
```

- 금액 문자열의 콤마를 **지우지 않고** 4자리를 찾는다 → `9,900` 이 코드로 오인되지 않음
- `deposits` 테이블에 `matched / unmatched / amount_mismatch / duplicate` 로 전부 기록
- 설정 절차는 `deposit-auto-approval.md`

## API

| 경로 | 인증 | 하는 일 |
|---|---|---|
| `/api/signup` `/api/login` `/api/me` | — | 가입·로그인·이용권 상태 |
| `/api/sample` | 없음 | 무료 회차 |
| `/api/content` | **이용권** | 전체 기출·교재 (미결제 402) |
| `/api/pay/request` | 로그인 | 코드 발급·계좌 안내·입금완료 알림·현금영수증 |
| `/api/hook/deposit` | `HOOK_SECRET` | 입금 알림 → 자동 승인 |
| `/api/admin/*` | `ADMIN_PASSWORD` | 목록·승인·해제·입금 로그 |
| `/api/save` `/api/load` | 로그인 | 풀이 기록 |

풀이 기록은 KV 에 통째로 넣는다. 그래서 **스키마를 바꿔도 워커를 고칠 필요가 없다.**

```js
S.seen[id] = { ok, no, last:'ok'|'no', at, noAt, pick }  // pick = 내가 고른 오답 보기
S.note[id] = '내 메모'
S.star[id] = true
```

## 교재 문체 — 수험서로 쓴다

읽는 사람은 시험을 준비하는 성인이다. 에세이가 아니라 **수험서**로 쓴다.
원고를 고친 뒤에는 반드시 `python tools/style_check.py` 를 통과시킨다.

- **평서 단정형.** “~한다 / ~이다 / ~로 본다.” 청유형(~해 보자, ~해 두자, ~외우자)을 쓰지 않는다.
- **화자를 드러내지 않는다.** 우리·여러분·필자·당신을 쓰지 않는다.
- **비유와 의인을 쓰지 않는다.** 조문은 덮치거나 붙잡거나 무너지지 않는다.
- **줄표(—)는 한 문장에 하나까지.** 나열은 쉼표·괄호·`<ul>` 로 한다.
- **한 문장에 한 논점.** 45자 안팎으로 끊는다.
- **절의 순서를 고정한다.** 의의 → 요건 → 효과 → 예외 → 비교표 → 기출.
- **근거를 문장 안에 적는다.** “(제4조 제1항)”, “(상법 제651조)”.
- **강조는 법정 용어와 숫자에만** `<b>` 를 쓴다. 감정 강조에 쓰지 않는다.
- **`bx e` 박스는 기출 분석 자리**다. 회차·번호·출제 형태를 실제 문항에서 확인하고 적는다.
  확인은 `python tools/find_q.py <낱말>` 로 한다. 없는 회차를 지어내지 않는다.

## 기출 ↔ 교재 연동

`tools/build_map.py` 가 600문항을 55개 절에 대응시켜 `content/exam_map.py` 를 만든다.
판정 근거는 조문 번호 일치와 절별 TF·IDF 낱말이고, 점수가 `FLOOR` 에 못 미치면
**아예 붙이지 않는다.** 틀린 연결은 없느니만 못하다. 손으로 바로잡을 때는 같은 파일의
`OVERRIDE` 에 `(회차, 과목, 번호): (과목, 장, 절)` 을 적는다.

```
교재 → 기출   book.html  절 끝의 '이 절에서 출제된 기출' → index.html?r=45&s=보험업법&n=1
기출 → 교재   index.html 채점 화면의 '교재에서 읽기'   → book.html?sec=보험업법|0|2
```

`content/exam_map.py` 는 산출물이다. 직접 고치지 말고 `build_map.py` 를 고쳐 다시 만든다.
절 데이터에는 `book_data.py` 가 `exq` 로 심고, 문항 쪽 색인은 `build_app.py` 가
`__SECLIST__` · `__QBOOK__` 로 박는다.

## 매일 진도 — 오늘 얼마나 했고, 지금 어디쯤인가

전략 대시보드 맨 위 칸이다. 채점할 때마다 `S.day['YYYY-MM-DD'] = {n, ok}` 에 눈금을 하나 쌓고(스냅샷 `v:4`, 계정에 함께 저장),
여기서 오늘 진도 · 연속 학습일 · 최근 2주 막대 · 주간 대비를 뽑는다. 하루 목표는 `GOAL`(20문항).

- **코치** — 선으로만 그린 얼굴(`face()`) 다섯 표정과 한마디. 상태에 따라 고른다(`coachOf`):
  3일 이상 쉬면 실망한 얼굴로 "포기하는 거야?", 오답이 쌓이면 걱정 얼굴로 "이렇게 하다가는 큰일나",
  목표를 넘기고 성적도 좋으면 "와~ 너 진짜 대단하구나!!", 점수가 낮으면 "괜찮아, 다 잘될 거야".
  잔소리만 남지 않도록 칭찬·위로를 반드시 섞는다.
- **잘하고 있는 것 / 지금 문제인 것** — 규칙으로 뽑는다(연속일·목표 달성·과락 과목·밀린 복습·안 푼 과목·표본 부족).
- **예상 성적과 등수** — 과목별 정답률을 100점으로 환산하고, 과목 40점·평균 60점 기준으로 판정한다.
  등수는 공식 등수가 없으므로 **추정**이다: 응시자 점수를 정규분포(σ=12 가정)로 보고
  `μ = 60 − z(1−합격률)·σ` 로 평균을 역산해 상위 %와 1만 명 중 등수를 낸다. 합격 가능성은 `Φ((내 점수−60)/8)`.
  손해사정사 1차 합격률은 공개 자료 기준 30% 안팎(2023년 32.7%)만 확인됐고 **연도별 공식 표는 확인하지 못했다** —
  기준선 세 개(실측·평균·보수)로 보여 주고 각주에 그렇게 적었다. 숫자를 고치면 각주도 같이 고칠 것.

## 전략 대시보드 — 오답을 교재 절 단위로 모은다

기출 앱의 세 번째 탭(`S.view === 'plan'`)이다. 서버 저장소를 새로 두지 않고 `S.seen`(문항별 정오답)과 `QBOOK`(문항 → 절)만으로 계산한다.
절마다 걸린 기출 수·푼 문항·틀린 횟수·못 잡은 오답을 세어 위험·주의·양호·안 풀어 봄으로 나눈다. 기준은 `planRows()` 주석과 화면 범례가 같아야 한다.

```
상황판 칸 → book.html?sec=과목|장|절&from=plan&miss=회차|과목|번호,…&t=푼 문항&a=푼 횟수&w=틀린 횟수
교재 '상황판으로' → index.html?view=plan
```

교재는 그 절 맨 위에 「이 절에서 채울 것」(`fixHtml`)을 세운다. 순서는 쉽게 이해하기 → 틀린 기출(절 끝 목록에서 펼치고 강조) →
틀린 문항의 용어·근거 조문 → 암기 카드 → 기출 앱에서 다시 풀기다. 단계 체크는 localStorage `sonsa:fix` 에만 남는다.
미리보기본(`dist/preview/exam.html`)은 600문항이 박혀 있어도 `S.full` 이 false 라서, 무료 안내는 `BANK.length >= 600` 으로 가린다.

### 2차 서술형 자동 채점

2차는 시행처가 정답을 공개하지 않는다. `exam2.html` 의 문항마다 「자동 채점」 버튼을 두고,
**모범답안(`content/ans2_*.py`)이 갖춘 것을 내 답안이 얼마나 담았는지**를 낱말로 대조한다. 축은 셋이다.

| 축 | 근거 | 기본 비중 |
|---|---|---|
| 핵심 논점 | `a.keys` | 60% |
| 근거 법령 | `a.law` | 20% |
| 숫자·수치 | 모범답안 본문의 금액·기간·비율 | 20% |

없는 축은 빼고 남은 축끼리 비중을 다시 나눈다(법령이 없는 문항은 논점 80 · 수치 20).
논점 하나는 그 안의 낱말(조사를 떼고 흔한 말을 버린 것)이 **모두 나오면 적중, 60% 이상이면 절반**이다.
답안 길이가 모범답안의 30% 미만이면 0.8배로 깎고 그 사실을 화면에 적는다. 점수는 배점 대비 0.5점 단위.
결과는 localStorage `sonsa2.score` 에만 남고 **계정 동기화는 하지 않는다**. 전략 대시보드의 「2차 서술형」 구획이 이 값을 읽는다.
모범답안이 있는 668문항(738문항 중)만 채점되고 나머지는 버튼이 꺼진다.
**낱말만 보므로 논리 전개와 구성은 재지 못한다** — 화면에도 그렇게 적어 두었다. 문구를 지우지 말 것.

### 지도 → 재작성 → 통과

채점만으로는 늘지 않는다. 문항 카드는 **작성 · 채점 · 지도 · 재작성 · 통과** 다섯 단계를 머리에 표시하고 지금 어디인지 보여 준다.

- **지도**(`coachHtml`)는 모범답안에서 근거를 끌어온다 — 뼈대(`a.plan`), 소문항 배점(`q.sub`),
  빠뜨린 논점마다 **그 논점이 나오는 모범답안 문장**(`findSent`), 빠진 근거 조문, 빠진 수치, 분량 목표.
- **재작성**은 두 번째 칸(`드래프트 id + '#2'`)에 쓴다. 처음 답안은 위에 그대로 두어 견주어 볼 수 있게 했다.
- **통과 기준 셋** — 득점률 70% 이상 · 빠뜨린 논점 0 · 분량이 모범답안의 40% 이상. 셋을 모두 넘겨야 통과다.
  분량 기준을 60%로 두었더니 **논점을 다 담은 만점 답안이 통과하지 못하는 모순**이 나와 40%로 내렸다(채점의 분량 감점선은 30%).
- 첫 점수와 채점 횟수를 남겨(`rec.fs` · `rec.n`) "처음 4점 → 지금 10점"을 보여 준다. `rec.p` 가 통과 여부다.

기준 숫자를 고치면 **화면 문구 · 이 문서 · 대시보드 2차 구획 설명** 세 곳을 함께 고칠 것.

## 디자인

s2s-lecture 토큰을 그대로 쓴다. 세 화면(`index` · `book` · `admin`)이 **같은 토큰 블록**을
복사해 쓰고 밤낮 모드 키도 `sonsa:theme` 로 공유한다. 한 곳을 바꾸면 세 곳을 같이 바꾼다.

- 배경 `#0A0A0C` · 카드 `#141417` · 경계 `#232328` · 은빛 5단계
- 사파이어(`--grad`)는 **주 버튼 한 곳에만**. 여기저기 쓰면 무너진다
- 제목은 `--crystal` 그라디언트 텍스트, 카드에 `--glass` `::before`
- 애플식 효과: 스크롤 헤어라인, IntersectionObserver 리빌, 스프링 이징(`--ease`),
  모바일 바텀시트 — **전부 `prefers-reduced-motion` 에서 정지**

## 알려진 함정

- **오답 모드에서 정답을 맞히면 화면이 튀던 버그**가 있었다. 채점 즉시 목록에서 빠지는데
  `S.pos` 는 그대로여서 다른 문항이 그려졌다. `S.pin` 으로 붙잡아 두는 방식으로 고쳤으니
  `list()` 나 `nextBtn` 을 손댈 때 이 동작을 깨뜨리지 말 것.
- `book.html` 은 `__BOOK__` 이 `null` 일 때 `/api/content` 로 받아 온다.
  `BOOK`/`TERMS`/`SEARCH`/`TLC` 를 재할당하지 말고 `Object.assign` / `push` 로 채워야 한다.
- 아티팩트로 발행할 때는 `build/build_artifact.py` 를 쓴다. Pretendard CDN 은 CSP 에 막힌다.

## 남은 일

- **문항별 해설** — `explanation` / `wrongWhy` 가 비어 있다(2026 보험업법 10문항만 시범).
  앱은 비면 "해설은 준비 중입니다"로 표시한다. 교재 55절을 근거로 인용하며 채우면 된다.
- **시행령 조문 대조** — law.go.kr 에서 시행령 본문을 못 가져왔다. 법제처 Open API 키 필요.
- **계좌 정보** — `worker/worker.js` 의 `BANK` 는 공인중개사 설정을 가져온 것. 확인 필요.
- **환불 규정** — 결제 화면 문구가 임시다.

## 배포

> **워커 이름은 `sonhaesajungsa` 하나다.** 한때 `sonhaesajeongsa` 가 함께 살아 있어,
> 비밀번호는 한쪽에 넣고 접속은 다른 쪽으로 해서 계속 안 맞는 사고가 났다.
> 두 워커는 같은 D1·KV 를 보고 있었고 데이터는 하나였다. 2026-09-09 에 `jeongsa` 를
> 삭제하고 이름을 통일했다. `--name` 을 손으로 치지 말고 저장소 폴더에서 실행한다.


> **배포 뒤에는 반드시 시크릿이 살아 있는지 확인한다.** 실제로 `wrangler deploy` 한 번에
> `ADMIN_PASSWORD` 가 떨어져 나간 적이 있다(`secret list` 에는 이름이 남아 있는데 워커는
> 값을 못 보는 상태). 확인은 값을 몰라도 된다 — 아무 값이나 넣어 보고 **401 이면 정상,
> 500 이면 시크릿이 빠진 것**이다.
> ```
> curl -s -X POST -H 'Content-Type: application/json' -d '{"password":"x"}' https://sonhaesajungsa.ykjjjs123.workers.dev/api/admin/login
> ```
> 빠졌으면 사용자가 `npx wrangler secret put ADMIN_PASSWORD` 를 다시 실행해야 한다.
> 비밀번호 값은 대신 넣지 않는다. `npm run deploy` 는 `--keep-vars` 를 붙여 둔다.


`main` 에 푸시하면 Cloudflare 대시보드의 **Workers Builds** 가 클론해서 `npx wrangler deploy`.
GitHub Actions 워크플로는 이중 배포를 막으려고 지웠다(되살리는 법은 README).
첫 배포 절차와 실패 원인표는 `README.md`.

---

## 작업 태도

- 가정을 말하고 시작한다. 해석이 갈리면 고르지 말고 묻는다.
- 요청한 것만 만든다. 안 시킨 기능·추상화·설정 가능성을 넣지 않는다.
- 고칠 것만 고친다. 옆의 멀쩡한 코드를 "개선"하지 않는다. 기존 스타일을 따른다.
- 내가 만든 고아 코드(안 쓰게 된 import·변수)는 치우고, 원래 있던 죽은 코드는 알리기만 한다.
- 200줄을 썼는데 50줄로 되겠으면 다시 쓴다.
- 끝냈다고 하기 전에 `npm run build` 와 (화면을 건드렸으면) `npm run verify` 를 통과시킨다.

## 법령 대조 — 조문 인용은 현행 원문과 맞춘다

교재의 `<div class="law">` 인용 상자는 `tools/check_law.py` 로 현행 법령 원문과 대조한다.
원문은 `data/law/`(법제처 OPEN API JSON: 보험업법·상법·보험업법 시행령)에 두고, 법이 바뀌면 `python tools/check_law.py --fetch` 로 새로 받는다.

```
python tools/check_law.py            # 전체 — 유사도 0.8 미만 또는 원문에 없는 숫자를 보고
python tools/check_law.py ex_bul_d.py
```

한계: 긴 인용 속 낱말 하나만 바뀐 경우(예: 책임준비금→해약환급금)는 유사도로 걸리지 않을 수 있다. 개정 조문은 낱말로도 찾는다.
2026-09-11 대조에서 바로잡은 대표 오류 — 제5조 사업계획서 3년·해약환급금 산출방법서(2022.12.31 개정), 제74조 제3항 7일, 제48조 상계 금지,
제97조 제3항 간주(1개월·6개월 모두), 제115조 승인·신고·보고(2020.12.8), 제185조·제189조 손해사정(2024.2.6), 제180조 민법 사단법인 준용.

본문 설명 문장(리드·본문·암기 카드·용어사전·쉽게 이해하기)은 `tools/check_text.py` 로 대조한다. 문장 속 조문 인용을 찾아
조문이 있는지·삭제됐는지, 문장의 숫자가 그 조(항)에 있는지, 핵심 낱말이 얼마나 겹치는지를 보고 `dist/check_text.txt` 에 쓴다.
`data/law/` 의 약관규제법·개인정보보호법·자배법·금소법 JSON 도 이 도구가 쓴다(`check_law.py --fetch` 는 세 법만 받는다).

```
python tools/check_text.py           # 요약 출력 + dist/check_text.txt
```

자동 판정이라 오탐이 섞인다. 같은 문장에 다른 조문의 숫자를 함께 적은 경우, 약관·판례 내용, 소제목 번호(「02 주관적…」)가 걸린다.
보고를 한 줄씩 원문과 읽고 고친다. 2026-09-11 에 바로잡은 것 — 겸영·부수업무 구분회계는 제11조의3(제12조는 국내사무소),
기금 현물납입 금지 제36조 제1항, 조직변경 특별결의 출석 2/3·발행주식 1/3, 손해사정사 등록취소는 제190조→제86조(필요적 제1항·임의적 제2항),
업무 개시 1개월은 시행령 제98조 제7항, 자동차 양도 낙부 10일은 상법 제726조의4, 수익자 지정·변경 동의는 제734조 제2항,
질병보험 준용은 제739조의3, 보험대리점 필요적 취소사유는 제88조 제1항의 다섯(제87조의3 제1항 위반 포함).
