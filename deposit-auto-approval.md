# 무통장 입금 → 자동 승인 (안드로이드 공기계 알림 포워딩)

토스뱅크 입금 푸시를 안드로이드가 읽어 서버로 보내면, 서버가 **입금자명 코드 + 금액(9,900원)** 을
맞춰 **자동으로 이용권을 켭니다.** PG 수수료 0원.

```
고객 입금(입금자명 뒤 4자리 코드) → 연지우 토스 계좌
   → 공기계 토스앱 입금 푸시 → MacroDroid 가 알림을 웹훅으로 전송
   → 서버가 코드·금액 매칭 → 자동 승인 (관리자 개입 0)
```

## 공인중개사와 같은 공기계를 쓸 때 ⚠️

두 서비스는 **워커도 D1 도 서로 다릅니다.** 한 대의 공기계로 둘 다 받으려면
**매크로를 두 개** 만들고, 각 매크로에 **금액 필터**를 걸어 주세요.

| | 공인중개사 | 손해사정사 |
|---|---|---|
| 금액 | **5,500원** | **9,900원** |
| 알림 내용 필터 | `5,500` 포함 | `9,900` 포함 |
| 웹훅 URL | 공인중개사 워커 `/api/hook/deposit` | 손해사정사 워커 `/api/hook/deposit` |
| HOOK_SECRET | 각 워커에 따로 설정 | |

필터를 걸지 않아도 **코드와 금액이 둘 다 맞아야 승인**되므로 잘못 열리는 일은 없습니다.
다만 필터가 없으면 상대 서비스의 입금이 `미매칭` 으로 로그에 쌓여 관리 화면이 지저분해집니다.

## 준비물
- 남는 **안드로이드 공기계** 1대 (상시 충전 ON)
- 그 폰의 **토스뱅크 앱 로그인** + **입금 알림 켜짐**
- 무료 앱 **MacroDroid** (Play 스토어)

## 1단계 — 서버 비밀키 설정 (1회)
```bash
npx wrangler secret put HOOK_SECRET
npx wrangler secret put ADMIN_PASSWORD
```
`HOOK_SECRET` 은 아무나 못 맞추는 무작위 문자열로 (예: `9Kx4Lp8Zr3tj9Q2m`).
이 값을 아래 MacroDroid 에도 **똑같이** 넣습니다. 외부 노출 금지.

## 2단계 — 공기계 MacroDroid 설정
1. MacroDroid 설치 → 실행 → **알림 접근 권한 허용**
2. **매크로 추가(+)**
3. **트리거**: `기기 이벤트 → 알림 → 알림 수신`
   - 애플리케이션: **토스뱅크**
   - 내용 필터: **`9,900`** (공인중개사 매크로와 구분하려면 필수)
4. **액션**: `연결 → HTTP 요청`
   - 방식 **POST**
   - URL: `https://<손해사정사-워커>/api/hook/deposit`
   - 콘텐츠 유형: `application/json`
   - 본문:
     ```json
     {"secret":"여기에_1단계_비밀키","text":"{알림내용}"}
     ```
     > `{알림내용}` 은 직접 타이핑하지 말고 본문 입력칸의 **매직 텍스트(`{}` 아이콘)
     > → 트리거 → 알림 내용(Notification Content)** 을 눌러 삽입하세요.
5. **저장 → 매크로 활성화(ON)**
6. 설정 → 배터리 → **MacroDroid 배터리 최적화 예외**

## 3단계 — 테스트
본인이 **9,900원을 입금자명 뒤 코드 붙여**(예: `테스트4821`) 소액 입금해 보거나,
`/admin.html` → **자동 입금 감지** 탭에서 신호가 `✅ 자동승인` 으로 뜨는지 확인합니다.

## 동작 규칙 (안전장치)

| 상황 | 결과 |
|---|---|
| 4자리 코드가 **대기 중** 결제건과 일치 + 금액이 **정확히 9,900원** | `✅ 자동승인` — 1년 이용권 ON |
| 코드는 맞는데 금액이 다름 | `⚠️ 금액불일치` — 자동승인 안 함, 관리자 확인 |
| 코드가 없거나 안 맞음 | `⚠️ 미매칭` — 관리자가 수동 승인 |
| 이미 승인된 코드 재신호 | `↩ 중복` — 무시 |

- 금액에 콤마를 지우지 않고 4자리를 찾으므로 `9,900` 자체는 코드로 오인되지 않습니다.
- 모든 신호는 `/admin.html` **자동 입금 감지** 표에 원문까지 기록됩니다.
- 이미 이용권이 살아 있는 계정이 다시 입금하면 **남은 기간에 1년이 더해집니다**(연장).

## 문제 대비
- 공기계가 꺼지거나 알림을 놓쳐도 `/admin.html` 에서 **수동 승인**하면 됩니다.
  자동은 편의, 수동은 항상 백업.
- 비밀키가 노출된 것 같으면 `npx wrangler secret put HOOK_SECRET` 로 새 값을 넣고
  MacroDroid 본문도 함께 갱신하세요.
