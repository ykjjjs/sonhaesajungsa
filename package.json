{
  "name": "sonhaesajungsa",
  "private": true,
  "description": "손해사정사 1차 기출 600문항 + 전자교재 55절 + 2차 기출 598문항 — Cloudflare Workers + D1 + KV",
  "scripts": {
    "build": "python3 build/build_exam.py && python3 build/build_book.py && python3 build/build_app.py && python3 build/build_app2.py",
    "artifact": "python3 build/build_artifact.py",
    "verify": "node test/verify_note.js && node test/verify_pay.js && node test/verify_admin.js && node test/verify_book.js",
    "serve": "python3 -m http.server 8788 --directory public",
    "deploy": "npx wrangler deploy --keep-vars",
    "build2": "python3 build/build_exam2.py && python3 build/build_app2.py"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
